spring:
  config:
    activate:
      on-profile: local
  datasource:
    password: ${SPRING_DATASOURCE_PASSWORD:1234}
  data:
    redis:
      password: ${SPRING_DATA_REDIS_PASSWORD:mjc813pass}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  servlet:
    session:
      cookie:
        secure: false

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost,http://127.0.0.1,http://localhost:3000,http://127.0.0.1:3000}
